/*
------------------------------------------------------------------------------ 
This code was generated by Amplication. 
 
Changes to this file will be lost if the code is regenerated. 

There are other ways to to customize your code, see this doc to learn more
https://docs.amplication.com/how-to/custom-code

------------------------------------------------------------------------------
  */
import * as common from "@nestjs/common";
import * as swagger from "@nestjs/swagger";
import { isRecordNotFoundError } from "../../prisma.util";
import * as errors from "../../errors";
import { Request } from "express";
import { plainToClass } from "class-transformer";
import { ApiNestedQuery } from "../../decorators/api-nested-query.decorator";
import { DownloadService } from "../download.service";
import { DownloadCreateInput } from "./DownloadCreateInput";
import { Download } from "./Download";
import { DownloadFindManyArgs } from "./DownloadFindManyArgs";
import { DownloadWhereUniqueInput } from "./DownloadWhereUniqueInput";
import { DownloadUpdateInput } from "./DownloadUpdateInput";

export class DownloadControllerBase {
  constructor(protected readonly service: DownloadService) {}
  @common.Post()
  @swagger.ApiCreatedResponse({ type: Download })
  async createDownload(
    @common.Body() data: DownloadCreateInput
  ): Promise<Download> {
    return await this.service.createDownload({
      data: data,
      select: {
        createdAt: true,
        id: true,
        updatedAt: true,
      },
    });
  }

  @common.Get()
  @swagger.ApiOkResponse({ type: [Download] })
  @ApiNestedQuery(DownloadFindManyArgs)
  async downloads(@common.Req() request: Request): Promise<Download[]> {
    const args = plainToClass(DownloadFindManyArgs, request.query);
    return this.service.downloads({
      ...args,
      select: {
        createdAt: true,
        id: true,
        updatedAt: true,
      },
    });
  }

  @common.Get("/:id")
  @swagger.ApiOkResponse({ type: Download })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  async download(
    @common.Param() params: DownloadWhereUniqueInput
  ): Promise<Download | null> {
    const result = await this.service.download({
      where: params,
      select: {
        createdAt: true,
        id: true,
        updatedAt: true,
      },
    });
    if (result === null) {
      throw new errors.NotFoundException(
        `No resource was found for ${JSON.stringify(params)}`
      );
    }
    return result;
  }

  @common.Patch("/:id")
  @swagger.ApiOkResponse({ type: Download })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  async updateDownload(
    @common.Param() params: DownloadWhereUniqueInput,
    @common.Body() data: DownloadUpdateInput
  ): Promise<Download | null> {
    try {
      return await this.service.updateDownload({
        where: params,
        data: data,
        select: {
          createdAt: true,
          id: true,
          updatedAt: true,
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new errors.NotFoundException(
          `No resource was found for ${JSON.stringify(params)}`
        );
      }
      throw error;
    }
  }

  @common.Delete("/:id")
  @swagger.ApiOkResponse({ type: Download })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  async deleteDownload(
    @common.Param() params: DownloadWhereUniqueInput
  ): Promise<Download | null> {
    try {
      return await this.service.deleteDownload({
        where: params,
        select: {
          createdAt: true,
          id: true,
          updatedAt: true,
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new errors.NotFoundException(
          `No resource was found for ${JSON.stringify(params)}`
        );
      }
      throw error;
    }
  }
}
